kind: 'Changed'
body: 'refreshed `.github/copilot-instructions.md` to correct the configuration-struct list in `internal/domain/entities/settings.go`: `ProviderConfig` is a re-export alias for gitforge''s type and there is no `RefreshCommand` struct — `refresh` is a `*bool` and the recipes are compile-time constants'
time: '2026-09-01T16:59:29.925591203Z'
